<div class="">
  <div class="row">
    <div class="col-md-9 col-xs-12">
        <%= form_for(@recorrido) do |f| %>
          <% if @recorrido.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@recorrido.errors.count, "error") %> prohibited this recorrido from being saved:</h2>

              <ul>
              <% @recorrido.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
              </ul>
            </div>
          <% end %>

          <div class="field">
            <%= f.label :Nombre %><br>
            <%= f.text_field :ruta_name,class:"form-control" %>
          </div>
          <div class="field">
            <%= f.label :Descripcion %><br>
            <%= f.text_area :ruta_description,class:"form-control" %>
          </div>
          <div class="field">
            <%= f.label :Recorrido %><br>
            <%= f.text_area :ruta_recorrido,class:"form-control box-info",id:"informacion" %>
          </div>
          <div class="actions pull-right" style="margin-top:20px;">
            <%= f.button 'Guardar',class:"btn btn-primary" %>
          </div>
        <% end %>
      </div>
  </div>
</div>
