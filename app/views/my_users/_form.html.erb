<%= form_for(@my_user) do |f| %>
  <% if @my_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_user.errors.count, "error") %> prohibited this my_user from being saved:</h2>

      <ul>
      <% @my_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_name %><br>
    <%= f.text_field :user_name %>
  </div>
  <div class="field">
    <%= f.label :user_password %><br>
    <%= f.text_field :user_password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
