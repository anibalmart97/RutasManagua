<%= form_for(@section) do |f| %>
  <% if @section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% @section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field col-xs-12">
      <%= f.label :section_name %><br>
      <%= f.text_field :section_name,class:"form-control" %>
    </div>

    <div class="field col-xs-12" style="margin-bottom:15px;">
      <%= f.label :'Descripcion' %><br>
      <%= f.text_area :section_description,class:"form-control box-info",id:"informacion" %>
    </div>

    <br>
    <br>

    <div class="actions">
      <%= f.submit  class:"btn btn-info pull-right"%>
    </div>
<% end %>
